import React from 'react';
import { TextEditor } from '../components/TextEditor';

// Page for editing a single document
function DocumentPage() {
    return <TextEditor />;
}

export default DocumentPage;